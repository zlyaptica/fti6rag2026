Чтобы бот не висел при нескольких запросах:

- В `run.py` создаётся очередь `llm_queue`, куда все запросы к LLM попадают.
- Постоянно осматривается эта очередь
- Бот при получении сообщения:
  - не вызывает LLM сразу,
  - а кладёт задачу в очередь,
  - и сразу отвечает, что запрос принят.
